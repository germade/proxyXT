import { h } from "preact";
import { StyledFooterProxyLabel } from "./FooterProxyLabel.styles.jsx";

export function FooterProxyLabel({ children }) {
  return <StyledFooterProxyLabel>{children}</StyledFooterProxyLabel>;
}